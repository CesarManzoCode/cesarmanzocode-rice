// Waybar layout — functional/structural only. Colors live in
// themes/<name>/waybar/colors.css, imported by style.css.
//
// arctic-glass structural override (see docs/themes/arctic-glass.md
// "Structure"): the mockup's "floating top bar — thin, centered content,
// lots of horizontal air" — same modules/on-click/exec as the shared bar,
// only margins/spacing change so the bar reads as a small glass card
// floating well clear of the screen edges, not a wall-to-wall strip.
{
  "layer": "top",
  "position": "top",
  "height": 34,
  "margin-top": 14,
  "margin-left": 120,
  "margin-right": 120,
  "spacing": 10,

  "modules-left": ["custom/launcher", "hyprland/workspaces"],
  "modules-center": ["clock"],
  "modules-right": ["pulseaudio", "network", "custom/cliphist", "tray", "custom/notification", "custom/power"],

  "custom/launcher": {
    "format": "󰣇",
    "tooltip": false,
    "on-click": "rofi -show drun"
  },

  "hyprland/workspaces": {
    "format": "{id}",
    "persistent-workspaces": { "*": 5 }
  },

  "clock": {
    "format": "{:%H:%M  ·  %a %d %b}",
    "tooltip-format": "<big>{:%Y %B}</big>\n<tt>{calendar}</tt>"
  },

  "pulseaudio": {
    "format": "{icon}  {volume}%",
    "format-muted": "  muted",
    "format-icons": { "default": ["", "", ""] },
    "on-click": "pactl set-sink-mute @DEFAULT_SINK@ toggle",
    "on-scroll-up": "wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 2%+",
    "on-scroll-down": "wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%-"
  },

  "network": {
    "format-wifi": "  {signalStrength}%",
    "format-ethernet": "󰈀  Wired",
    "format-disconnected": "󰤭  offline",
    "format-linked": "󰈀  Wired (no IP)",
    "tooltip-format-wifi": "{essid}  ·  {signalStrength}%\n{ifname}: {ipaddr}/{cidr}",
    "tooltip-format-ethernet": "{ifname}: {ipaddr}/{cidr}",
    "tooltip-format-disconnected": "Disconnected"
  },

  "custom/cliphist": {
    "format": "",
    "tooltip-text": "Clipboard history",
    "on-click": "cliphist list | rofi -dmenu | cliphist decode | wl-copy"
  },

  "tray": {
    "icon-size": 16,
    "spacing": 8
  },

  // Requires the swaync component; if it's disabled on this machine,
  // swaync-client is simply absent and the click is a no-op — same
  // best-effort convention already used by custom/cliphist above (see
  // README "Known limitations"). "alt"/"class" values come straight from
  // `swaync-client -swb`'s own JSON output — verify with that command on
  // the real machine if the icon ever looks wrong for the DND state.
  "custom/notification": {
    "exec": "swaync-client -swb",
    "return-type": "json",
    "format": "{icon}",
    "format-icons": {
      "notification": "󰂚",
      "none": "󰂜",
      "dnd-notification": "󰂛",
      "dnd-none": "󰂛"
    },
    "on-click": "swaync-client -t -sw",
    "on-click-right": "swaync-client -d -sw",
    "escape": true,
    "tooltip": true
  },

  "custom/power": {
    "format": "⏻",
    "tooltip": false,
    "on-click": "~/.config/rofi/power-menu.sh"
  }
}
